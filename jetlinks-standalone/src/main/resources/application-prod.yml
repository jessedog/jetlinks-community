spring:
    resources:
        static-locations: file:./index/, file:./static/,/,classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/, classpath:/public/
    redis:
        host: 121.62.23.178
        port: 11449
        lettuce:
            pool:
                max-active: 1024
        timeout: 20s
    r2dbc:
        url: r2dbc:mysql://121.62.23.178:11447/jetlinks?ssl=false&serverZoneId=Asia/Shanghai
        #    url: r2dbc:mysql://127.0.0.1:3306/jetlinks?ssl=false&serverZoneId=Asia/Shanghai # 修改了配置easyorm相关配置也要修改
        username: root
        password: uestc208
        pool:
            max-size: 32
    elasticsearch:
        uris: 121.62.23.178:51446
        socket-timeout: 10s
        connection-timeout: 15s
        webclient:
            max-in-memory-size: 100MB
easyorm:
    default-schema: jetlinks # 数据库默认的schema
    dialect: mysql #数据库方言
tdengine:
    enabled: true
    database: jetlinks
    restful:
        endpoints:
            - http://121.62.23.178:51454/
        username: root
        password: taosdata
elasticsearch:
    embedded:
        enabled: false # 为true时使用内嵌的elasticsearch,不建议在生产环境中使用
        data-path: ./data/elasticsearch
        port: 9201
        host: 0.0.0.0
    index:
        default-strategy: time-by-month #默认es的索引按月进行分表, direct则为直接操作索引.
        settings:
            number-of-shards: 1 # es 分片数量
            number-of-replicas: 0 # 副本数量
device:
    message:
        writer:
            time-series:
                enabled: true #写出设备消息数据到elasticsearch
logging:
    level:
        com.github.tonivade: error